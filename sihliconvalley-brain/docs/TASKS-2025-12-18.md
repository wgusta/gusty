# Tasks 2025-12-18

## Immediate
- Fill `.env` secrets: `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, `N8N_ENCRYPTION_KEY` (32-hex), `N8N_BASIC_AUTH_USER/PASSWORD`, `POSTGRES_PASSWORD`, `NTFY_TOPIC`; optional `GITHUB_TOKEN`, `RESEND_API_KEY`.
- Restart stack: `docker compose down && docker compose up -d`.
- In n8n, create PostgreSQL credential (`Brain PostgreSQL`): host `postgres`, db `sihliconvalley_brain`, user `brain`, password from `.env`.

## Tests to run
- In n8n, manual run:
  - Daily Health Check (writes to `health_checks`, sends ntfy on failure).
  - Board Meeting (lean, single-call; stores minutes to `board_meetings` and ntfy alert).
- CLI: `docker compose ps`, `docker compose logs -f n8n` to confirm no credential errors.

## Optional follow-ups
- Bring back email assistant if needed (`archive/02-executive-assistant.json`).
- Re-enable reverse proxy/HTTPS with `archive/Caddyfile` if exposing publicly.
- Add GitHub token for gist backups if you re-add that branch.
